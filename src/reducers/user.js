const initialState = {
  email: 'vincent@gmail.com',
  password: 'aze',
  infos: {
    pseudo: '',
  },
};

const user = (state = initialState, action = {}) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default user;
